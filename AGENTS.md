# 成蹊云开发协作规则

## 算力使用原则

- 优先把高能力模型和推理额度用于架构取舍、复杂业务规则、疑难排错、代码实现和安全审查。
- 状态查询、找文件、查看路径、解释命令、整理简短记录等工作保持轻量；不得为这类任务启动子 Agent 或扫描整段历史会话。
- 恢复工作时按固定顺序读取：`docs/WORK_STATUS.md`、当前 Git 状态、当前任务对应的计划/规格、将要修改的代码。除非用户明确要求核对历史，不读取 `.codex` 会话记录或无关旧计划。
- 只有两个以上真正独立、能并行产生代码或审查成果的任务才使用子 Agent。机械任务选用较省额度的模型；高能力模型留给跨模块设计、复杂调试和最终审查。
- 开发过程中先运行与改动直接相关的测试。只有到达提交、里程碑、推送或涉及公共接口时才运行完整测试；已通过且代码未变化的测试不重复运行。
- 工具输出保持精简：优先输出失败信息、统计和结论，不打印无关的大段日志或完整历史。
- 连续完成一批相关工作后再汇报，避免用多个只包含“继续”的回合重复加载上下文。

## 恢复与交接

- GitHub 是代码事实来源，`docs/WORK_STATUS.md` 是恢复工作的第一入口。
- 每个实质性里程碑完成后更新 `docs/WORK_STATUS.md`，只记录当前分支、最新提交、已验证能力、未决阻塞和下一项工作；保持简短，不复制完整计划或测试日志。
- 限额恢复后从交接文件和 Git 提交继续，不从头分析整个仓库，不重复已完成任务。
- 外部账号授权、真实业务资料、不可逆操作或会改变项目方向的决定才需要用户处理；其他已授权开发工作自行推进。

## 数据与项目边界

- 成蹊云 CRM 与 NEXORA 是两个独立项目，不得混用资料、仓库、设计或结论。
- CRM 演示和测试只使用虚构数据，不写入真实客户、身份、支付、合同或学员资料。
- 保留 legacy 应用基线；未经明确授权，不删除项目、工作树、分支或历史资料。
